{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run server",
      "type": "shell",
      "command": "php artisan serve",
      "options": { "cwd": "${workspaceFolder}/core4-back" },
      "problemMatcher": []
    },
    {
      "label": "Run queue",
      "type": "shell",
      "command": "php artisan queue:work",
      "options": { "cwd": "${workspaceFolder}/core4-back" },
      "problemMatcher": []
    }
  ]
}